{
    "env": {
        "_comment": "Never run the server in production with debug!",
        "debug": true,
        "logLevel": 1
    },

    "http": {
        "port": 3001
    },

    "db": {
        "host": "127.0.0.1",
        "port": 3306,
        "user": "altnnas",
        "pass": "altnnas",
        "name": "altnnas-db"
    },

    "encryption_keys": {
        "_comment": "First value is the AES-256 key, second is the AES-256 IV",
        "service_token": [ "aes256encryptionkey", "aes256encryptioniv" ],
        "saltRounds": 12
    }
}